import XCTest
@testable import Loopa

final class VideoProcessingServiceTests: XCTestCase {
    func testInit() {
        let service = VideoProcessingService()
        XCTAssertNotNil(service)
    }
    
    // Add more tests for video processing as needed
}
